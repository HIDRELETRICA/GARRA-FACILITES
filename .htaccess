RewriteEngine On

# Redirecionar HTTP para HTTPS
RewriteCond %{SERVER_PORT} 80
RewriteRule ^(.*)$ https://garrafacilites.com.br/$1 [R,L]

# Reescrever URL para index.html
RewriteRule ^$ index.html [L]

# Reescrever URL para trabalhos.html
RewriteRule ^trabalhos$ trabalhos.html [L]
